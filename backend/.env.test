# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
# Use same MySQL database as dev (DAMA bundle uses transactions + rollback)
# Tests won't affect dev data because they're rolled back automatically
DATABASE_URL="mysql://maintly:secret@mysql:3306/maintly?serverVersion=8.0&charset=utf8mb4"
