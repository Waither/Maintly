# Local production-like preview values (vite build/preview)
# In vite preview mode, use explicit backend URL (preview server does not proxy API like dev server).
VITE_API_URL=http://localhost:8000/api

# Leave empty by default (no dedicated WS server on :8000).
VITE_WS_URL=

VITE_USE_HTTPS=false
