import { RowsType } from '../../types';
import { useActiveDataProps } from './types';
export declare const useActiveData: ({ dataColumns, dataRows, sort, searchValue, advancedSearch, advancedSearchValue, }: useActiveDataProps) => RowsType;
export default useActiveData;
